import React from 'react';
import { MenuScreen } from '../../src/screens/MenuScreen';

/**
 * "Cardápio" tab — renders the Menu screen.
 */
export default function MenuRoute() {
  return <MenuScreen />;
}
